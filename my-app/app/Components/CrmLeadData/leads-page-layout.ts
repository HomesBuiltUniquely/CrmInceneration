/** Shared width + horizontal padding for leads page sections (heatmap, toolbar, table). */
export const LEADS_PAGE_CONTAINER_CLASS = "mx-auto w-full max-w-[1400px] px-4 md:px-6";

/** Tighter left inset so header branding sits near the left border. */
export const LEADS_PAGE_HEADER_CLASS =
  "mx-auto w-full max-w-[1400px] pl-0 pr-2 md:pl-1 md:pr-6";

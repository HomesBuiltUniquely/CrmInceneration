# New CRM — Presales tracking & read-only (companion)

This file is linked from [PRESALES_ROLES_MASTER_GUIDE.md](./PRESALES_ROLES_MASTER_GUIDE.md).

**Placeholder:** Add the verified → sales handoff content here when you migrate it from your internal wiki or ticket.

Suggested sections to document:

- `PRE_SALES` and `PRESALES_EXECUTIVE` shared list rules
- `presalesTrackingReadOnly` behaviour
- fullName / username matching for assignee visibility
- `GET /v1/FormLead/details/{id}` (and other lead types) for read-only presales views
- differences vs legacy UI

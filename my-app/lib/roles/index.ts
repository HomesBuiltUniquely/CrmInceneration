/**
 * Role- and presales-pool helpers. Prefer `import { … } from "@/lib/roles"` in new code.
 * Implementation files stay in `lib/` root for backward compatibility.
 */
export * from "../crm-role-access";
export * from "../presales-lead-visibility";
